name: open_website
handler: handlers.pc_actions.open_website
slots:
  - name: site_name
    type: str
    required: false

slot_hints:
  site_name:
    patterns:
      - '(?:открой|запусти|зайди на)\s+(?:сайт\s+)?(?P<site_name>.+)'